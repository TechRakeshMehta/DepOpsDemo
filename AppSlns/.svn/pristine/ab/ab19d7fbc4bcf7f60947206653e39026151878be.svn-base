import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LsAliasInfoComponent } from './ls-aliasInfo.component';

describe('LsAliasInfoComponent', () => {
  let component: LsAliasInfoComponent;
  let fixture: ComponentFixture<LsAliasInfoComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LsAliasInfoComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LsAliasInfoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
