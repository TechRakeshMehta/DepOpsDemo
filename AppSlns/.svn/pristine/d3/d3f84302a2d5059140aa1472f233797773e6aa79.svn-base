 <div id="bodyBlock" class="container-fluid">
  <section class="p-2">
  <form [formGroup]="reactiveForm">
    <re-captcha formControlName="recaptchaReactive" #captchaRef="reCaptcha" size="invisible"  (resolved)="$event && submit($event)"></re-captcha>
    <button (click)="captchaRef.execute()">Submit</button>
    <button type="button" class="btn btn-primary" (click)="openModal(template)">Create template modal</button>
  </form> 
  <!-- <div  bsModal #lgModal="bs-modal" class="modal fade" tabindex="-1"
     role="dialog" aria-labelledby="dialog-sizes-name1">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header">
        <!-- <h4 id="dialog-sizes-name1" class="modal-title pull-left">Large modal</h4> -->
        <!-- <button type="button" class="close pull-right" (click)="lgModal.hide()" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <carousel [showIndicators]="False" [interval]="-1" >
          <slide *ngFor="let a of Locations">
            <img src="data:image/jpg;base64,{{a.LocationImages}}" alt="first slide" style="display: block; width: 100%;">
          </slide>
        </carousel> 
      </div>
    </div>
  </div>
</div>  -->
<ng-template #template>
  <div class="modal-header">
    <!-- <h4 class="modal-title pull-left">Modal</h4> -->
    <button type="button" class="close pull-right" aria-label="Close" (click)="modalRef.hide()">
      <span aria-hidden="true">&times;</span>
    </button>
  </div>
  <div class="modal-body">
    <carousel [showIndicators]="False" [interval]="-1" >
      <slide *ngFor="let a of Locations">
        <img src="data:image/jpg;base64,{{a.LocationImages}}" alt="first slide" style="display: block; width: 100%;height:400px">
      </slide>
    </carousel> 
  </div>
</ng-template>

</section>
</div>

    

  
